# Frontend Production Environment Variables Template
# For single-service deployment on Render

# Backend API URL
# Leave EMPTY for same-origin deployment (backend serves frontend)
# The frontend will make API calls to the same domain
VITE_BACKEND_HOST_URL=
